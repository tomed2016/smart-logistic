customer:
  service:
    base-url: http://localhost:8081

geo:
  catalog:
    service:
      base-url: http://localhost:8082

# Sin servidor embebido (CucumberSpringConfiguration usa webEnvironment = NONE):
# este puerto nunca se usa, pero se fija explicitamente para evitar advertencias.
server:
  port: -1

logging:
  level:
    root: WARN
    cl.smartlogistic.qa: INFO
